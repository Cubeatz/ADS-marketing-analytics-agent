---
description: 首次使用门禁。未完成 onboarding 时，Agent 必须自动检查环境并引导配置。
alwaysApply: true
---

# 首次使用门禁

如果 `config/workspace.json` 不存在，或 `onboarding.completed` 不为 `true`：

1. 停止所有分析和报告生成。
2. 自动运行环境检查。
3. 环境就绪后自动启动首次配置流程。
4. 根据用户实际选择的平台生成 MCP 配置。
5. 只有缺少平台 OAuth、API Token、账户 ID 或系统权限时，才让用户介入。

不要把脚本命令直接丢给非技术用户。用户只需要说“帮我完成首次配置”。

## 问卷规则

- 每题默认推荐项为 A。
- 输入 Z 或“跳过”等同选择 A。
- 第 1 题平台不可跳过。
- 第 2 题选择“一次性使用”时，自动跳过报告时间、工作日、自动计划任务三题。
- 第 1 题只展示 `config/ad-platforms.json` 中已支持的平台。

## 用户可见说法

推荐对用户这样说：

```text
我先帮你检查环境，然后用几个问题确认平台和日报方式。需要你登录广告后台的地方，我会告诉你去哪里复制账户 ID 或 token。
```
