# Claude Code 项目指令

本项目的 Agent 行为定义见 [AGENTS.md](./AGENTS.md)。

- 只读分析 Google Ads、Meta Ads、AppsFlyer
- 无数据库，配置在 `config/`
- 报告输出到 `reports/`

安装 MCP：`bash scripts/install.sh claude` 或 `powershell scripts/install.ps1 -Ide claude`
